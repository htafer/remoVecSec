"""This module is responsible for reading and
processing fastq files.
"""
import os.path
import sys
import random


def samplefastq(filename,num_reads):
"""
Read a fastq file and return a sampled fastq 
with the number of reads num_reads.

Args:
   filename (str): filename of the fastq file to read. Already checked if it exists.
   num_reads(int): number of reads to sample

Returns:
   a list of reads in fastq format that can be further processed
"""

#Should check if it is a real fastq file. Up to the user

f = open(filename,'r')




